name: mom6-diagnostics-manager
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - pandas>=1.3.0
  - ipywidgets>=7.6.0
  - ipython>=7.16.0
  - jupyter>=1.0.0
  - notebook>=6.0.0
  # Optional development dependencies
  - pytest>=6.0
  - pytest-cov>=2.10
  - black>=21.0
  - flake8>=3.9
  - mypy>=0.900
